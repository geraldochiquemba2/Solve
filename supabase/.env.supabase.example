# Supabase Edge Function secrets (set via: supabase secrets set KEY=value)
# NÃO commitar valores reais — usar supabase secrets set em produção

# URL do CRM (o edge function empurra leads para aqui)
CRM_API_URL=https://solve-sqoh.onrender.com

# API Key do CRM (deve ser o mesmo API_KEY definido no Render)
CRM_API_KEY=solve-crm-api-key-2024
